@extends('admin.layouts.app')
@section('content')
@include('admin.analytics._header', ['title' => 'Inventory analytics', 'description' => 'Stock value, stock-outs and inventory movement.'])
<div class="row"><div class="col-lg-6 mb-4"><div class="card h-100"><div class="card-header pb-0"><h6>Inventory sold</h6></div><div class="card-body px-0"><table class="table mb-0"><thead><tr><th>Product</th><th class="text-end">Units</th></tr></thead><tbody>@forelse($recentlySold as $product)<tr><td class="px-4 text-sm">{{ $product->name }}</td><td class="text-end px-4">{{ number_format($product->units) }}</td></tr>@empty<tr><td colspan="2" class="text-center py-4">No sales in period.</td></tr>@endforelse</tbody></table></div></div></div>
<div class="col-lg-6 mb-4"><div class="card h-100"><div class="card-header pb-0"><h6>Items remaining 1 in quantity</h6><p class="text-xs text-secondary mb-0">Current products with exactly one unit left</p></div><div class="card-body px-0"><table class="table mb-0"><thead><tr><th>Product</th><th class="text-end">Quantity</th></tr></thead><tbody>@forelse($oneRemaining as $product)<tr><td class="px-4"><a href="{{ route('products.edit', $product->id) }}" class="text-sm font-weight-bold">{{ $product->name }}</a><div class="text-xs text-secondary">{{ $product->sku ?: 'No SKU' }}</div></td><td class="text-end px-4 font-weight-bold">{{ number_format($product->quantity) }}</td></tr>@empty<tr><td colspan="2" class="text-center py-4">No products currently have exactly one unit left.</td></tr>@endforelse</tbody></table></div></div></div></div>
@endsection
